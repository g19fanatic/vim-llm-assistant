# fix_plan.md
- [ ] 1. Fix g:llm_log_level='none' being overridden to 'info': Remove the unconditional override at plugin/llm.vim:45-47 that forces 'none' to 'info'. Instead, respect the user's explicit choice — 'none' should disable ALL logging (no session.log, no request dirs, no response files). Verify that llm#log#startup_cleanup() at autoload/llm/log.vim:199 already has early-return guard for 'none'. Read plugin/llm.vim:40-50 first to see the current override block. Files: plugin/llm.vim:45-47 (remove override), autoload/llm/adapters/aichat.vim:191-194 (verify log guards).
- [ ] 2. Improve error messaging when opening debug-only logs at info level: In llm#log#open() at autoload/llm/log.vim:96-130, when filereadable() returns false for 'input' or 'aichat' types, check if g:llm_log_level !=# 'debug' and show a helpful message like "[LLM] <type> log requires debug level. Set g:llm_log_level='debug' in your vimrc or use :LLMLogDebug" instead of generic "[LLM] File not found: ...". Only show the generic message for 'response'/'tools' types (which should exist at all levels). Read autoload/llm/log.vim:110-130 first. Files: autoload/llm/log.vim:110-130.
- [ ] 3. Add :LLMLogTail tools and :LLMLogTail session support: Extend llm#log#tail() at autoload/llm/log.vim:138-169. Add 'tools' to l:filemap (maps to 'tools.log'). For 'session', handle specially — session.log is at llm#log#dir() . '/session.log' (root level, not per-request), so bypass the resolve_request_dir() logic when type=='session'. Update llm#log#complete_types() at line 56 to include these in completions (they're already listed but verify). Read autoload/llm/log.vim:138-170 first to understand the current tail structure. Files: autoload/llm/log.vim:138-170, autoload/llm/log.vim:56-63.
- [ ] 4. Add :LLMLogDebug toggle command for runtime log level switching: Create llm#log#toggle_debug() function that cycles g:llm_log_level between 'info' and 'debug' (and 'none' if user had it set). Show current level after toggle with echom. Add command definition 'LLMLogDebug' in plugin/llm.vim after line 90, using -nargs=0. Follow the pattern of other LLMLog* commands at plugin/llm.vim:87-90. Read plugin/llm.vim:87-90 and autoload/llm/log.vim end-of-file for placement. Files: autoload/llm/log.vim (append new function), plugin/llm.vim:87-90 (add command).
- [ ] 5. Add :LLMLogHistory [N] command to browse past request directories: Create llm#log#history(count) function that lists the N most recent request directories (default 10) with timestamp, model, prompt snippet (parsed from session.log or dir names), and lets user pick one to open. Use glob() with the YYYYMMDD_HHMMSS_NNN pattern (same as llm#log#clean uses at line 177). Display as numbered list via inputlist(), then open selected dir in netrw. Add command definition in plugin/llm.vim. Read autoload/llm/log.vim:175-180 for the glob pattern and autoload/llm/log.vim:132-136 for browse() pattern to follow. Files: autoload/llm/log.vim (new function), plugin/llm.vim:87-90 (add command).
- [ ] 6. Add :LLMLogSearch <pattern> for grep through session.log: Create llm#log#search(pattern) function that greps session.log for the pattern and displays results in a quickfix list. Use vimgrep or systemlist('grep -n ...') + setqflist(). Show [LLM] message with match count. Add command definition with -nargs=1 in plugin/llm.vim. Read session.log format from autoload/llm/adapters/aichat.vim:348-354 (the session_append entry format: 'datetime | model | duration | status | prompt'). Files: autoload/llm/log.vim (new function), plugin/llm.vim:87-90 (add command).
- [ ] 7. Add comprehensive "Log Access & Debugging" section to build_run_test.md: Document ALL LLMLog commands (including new ones added in tasks 1-6) with usage patterns, shell recipes for accessing logs from terminal, how to enable debug mode, aichat env vars (AICHAT_LOG_PATH, AICHAT_LOG_LEVEL), log file locations, and troubleshooting. Include: quick reference table of all commands, shell one-liners for common tasks, debug enablement instructions. Read the current build_run_test.md and the final state of plugin/llm.vim commands. Files: project_info/build_run_test.md (append new section).
